CREATE TABLE shelfs (
	id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	name VARCHAR(50) NOT NULL CONSTRAINT name_uniqueness CHECK (name <> '') UNIQUE,
	room VARCHAR(50) NOT NULL CHECK (room <> ''), 
	location VARCHAR(50) NOT NULL CHECK (location <> '')
);